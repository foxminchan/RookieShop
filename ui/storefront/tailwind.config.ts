const config = {
  content: ["./Pages/**/*.cshtml", "./Views/**/*.cshtml", "/Areas/**/*.cshtml"],
  theme: {
    extend: {},
  },
    plugins: [],
    variants: {
        extend: {
            display: ["group-hover"],
        },
    },
};

export default config;
