namespace RookieShop.Storefront.Constants;

public static class AuthScope
{
    public const string Read = nameof(Read);
    public const string Write = nameof(Write);
    public const string All = nameof(All);
}